# Stay Daytrevel — 콘텐츠 작성 규격

`stay.daytrevel.com`에 발행하는 한국어 여행 콘텐츠의 작성 규격입니다.
사람과 코딩 에이전트(Codex, Claude Code 등)가 공통으로 따릅니다.

배포·환경변수·애드센스 설정은 [README.md](./README.md)에 있습니다. 이 문서는 **글을 쓰는 규칙**만 다룹니다.

## 반복 작업: 새 글 한 편 추가하기

새 글을 추가할 때 하는 일은 **`.mdx` 파일 하나 만드는 것뿐**입니다.
목록 페이지, 사이트맵, FAQ 구조화 데이터, canonical, OG 태그는 전부 자동으로 붙습니다.

1. 컬렉션을 고른다.
   - **`src/content/stays/`** — 어느 지역에 숙소를 잡을지 고르는 글. 라우트 `/stays/{slug}/`
   - **`src/content/travel/`** — 가볼만한 곳, 코스, 축제, 시즌 정보. 라우트 `/travel/{slug}/`
2. `templates/stay.mdx` 또는 `templates/travel.mdx`를 복사한다.
3. 파일명은 소문자 영문 슬러그, 하이픈 구분. **그대로 URL이 된다.**
   예) `naejangsan-autumn-foliage-timing.mdx` → `/travel/naejangsan-autumn-foliage-timing/`
4. 프론트매터와 본문을 아래 규격대로 채운다.
5. `npm run build`로 검증한다. 스키마를 어기면 빌드가 실패한다.

한 번에 여러 편을 쓸 때도 파일만 늘리면 됩니다. 페이지나 설정은 건드리지 마세요.

## 프론트매터

정답은 `src/content.config.ts`입니다. 어기면 빌드가 깨집니다.

| 필드 | stays | travel | 규칙 |
| --- | --- | --- | --- |
| `title` | 필수 | 필수 | 타겟 키워드로 시작. `키워드: 부제` 또는 `키워드 - 부제` |
| `description` | 필수 | 필수 | 한 문장, 60~100자. `~정리했습니다.` / `~가이드입니다.`로 끝냄 |
| `keyword` | 필수 | 필수 | 검색 타겟 키워드 하나. `title` 앞부분과 일치시킬 것 |
| `pubDate` | 필수 | 필수 | `YYYY-MM-DD` (따옴표 없이) |
| `city` | 필수 | 필수 | 도시·지역명. 예) `'서울'`, `'정읍'`, `'오사카'` |
| `country` | 필수 | 필수 | 예) `'대한민국'`, `'일본'`, `'대만'`, `'홍콩'`, `'베트남'` |
| `season` | — | 선택 | 시즌 글에만. 예) `'10월 말·11월 초 가을'` |
| `affiliateReady` | 필수 | — | 신규 글은 `false` |
| `image` 외 3종 | 선택 | 선택 | 아래 "이미지" 참고. 넣을 거면 4개를 **세트로** |
| `faq` | 필수 | 필수 | **4개**가 관례. `q`/`a` 쌍 |

문자열은 작은따옴표로 감쌉니다. 값 안에 작은따옴표가 필요하면 `''`로 이스케이프합니다.

### 이미지

`image` / `imageAlt` / `imageCredit` / `imageSource`는 **넣으려면 4개를 모두** 넣습니다.
셋만 넣으면 출처 표기가 깨집니다. 확실한 이미지가 없으면 4개 모두 생략하세요.

- 출처는 라이선스가 명확한 곳만: Pexels, 한국관광공사(`tong.visitkorea.or.kr`), 공공기관
- `imageCredit`은 `'사진: 촬영자 / 출처'` 형식
- `imageAlt`은 장식이 아니라 실제 화면 설명

## 본문

### 문체

**존댓말(`~습니다`, `~입니다`)로 씁니다.** 본문과 FAQ 모두 동일합니다.

> 예외: 초기 6편(`osaka-shinosaka`, `osaka-tennoji`, `osaka-umeda`,
> `changgyeonggung-grand-greenhouse`, `sejong-lake-central-park`,
> `yangjaecheon-walking-course`)은 평서형으로 남아 있는 레거시입니다.
> **이걸 보고 따라 쓰지 마세요.** 새 글은 존댓말입니다.

### 구조

`#`(h1)은 쓰지 않습니다. 제목은 페이지가 자동으로 렌더합니다. 섹션은 전부 `##`입니다.

**stays** — 5~6개 섹션

```
## 한눈에 보는 요약          2~3문장. 위치 선택의 핵심 기준을 먼저 제시
## {지역} 숙소 지역별 특징     후보 지역마다 한 문단씩 (보통 3~4개)
## 이동 동선과 접근성         공항·역 이동, 캐리어·야간 동선
## 예약 전 체크리스트         불릿 5~6개
## {지역} 숙소 위치별 비교     표 (아래 형식)
## 예약 버튼을 넣기 전 필요한 정보
                            2~3문장 + 아래 면책 문단
## 다음 작업                 제휴 확정 시 보강할 내용 1~2문장
```

마지막 두 섹션은 제휴 준비용 자리표시자입니다. 다음 문단을 그대로 유지하세요.

```html
<p class="note">제휴 프로그램이 준비되면 이 위치에 예약 버튼과 실제 숙소 후보를 추가합니다.</p>
```

**travel** — 4~5개 섹션

```
## 한눈에 보는 요약          2~3문장. 이 장소가 어떤 방문에 맞는지
## {주제에 맞는 제목}         본문 섹션 1~2개. 시기, 코스, 동선 등
## 방문 전 체크리스트         불릿 5개
## {주제} 비교               표 (선택이지만 권장)
## 여행 계획 메모            2~3문장으로 마무리. 혼잡을 피하는 요령 등
```

travel에는 `<p class="note">` 면책 문단을 넣지 않습니다.

### 표

**마크다운 파이프 표 대신 아래 HTML 형식을 쓰세요.**
`table`에 `min-width: 640px`가 걸려 있는데 가로 스크롤은 `.table-wrap`만 제공합니다.
마크다운 표로 쓰면 모바일에서 페이지가 옆으로 밀립니다.

```html
<div class="table-wrap">
  <table>
    <thead>
      <tr><th>구분</th><th>잘 맞는 방문객</th><th>확인할 점</th></tr>
    </thead>
    <tbody>
      <tr><td>...</td><td>...</td><td>...</td></tr>
    </tbody>
  </table>
</div>
```

3열 4행 안팎이 적당합니다.

### FAQ

- `q`는 실제 검색 질문 형태로. "언제가 절정인가요?", "아이와 함께 가도 되나요?"
- `a`는 1~3문장, 존댓말
- 본문에 없는 새 정보를 넣지 말고, 본문 내용을 질문 형태로 다시 답하는 수준으로

## 사실관계 원칙 (중요)

- **가격, 요금, 영업시간, 운행 시간표, 별점, 객실 수는 쓰지 않습니다.** 금방 틀린 정보가 됩니다.
- 대신 "무엇을 확인해야 하는지"를 씁니다. 이 사이트의 가치는 최신 수치가 아니라 판단 기준입니다.
  - ✗ `케이블카는 오전 9시부터 오후 6시까지 운행합니다`
  - ✓ `케이블카 운행 시간과 매표 마감 시간을 미리 확인하세요`
- 시기는 단정하지 말고 예년 기준으로: `예년 기준 10월 말~11월 초에 절정을 이루는 경우가 많습니다`
- 확실하지 않은 고유명사(역 이름, 노선명, 시설명)는 쓰지 말고 아는 범위로 일반화하세요.
- **특정 호텔·숙소를 추천하거나 이름을 나열하지 않습니다.** 지역 단위로만 다룹니다.
- 개인 후기나 체험담을 지어내지 않습니다.

## 검증

```bash
npm install
npm run build
```

빌드가 통과하면 끝입니다. 실패하면 대부분 프론트매터 필수 항목 누락입니다.
확인하고 싶으면 `npm run preview` 후 새 글 URL을 열어보세요.

## 손대지 말 것

콘텐츠 추가 작업에서는 아래를 수정하지 않습니다. 구조 변경이 필요하면 사람에게 물으세요.

- `src/content.config.ts` (스키마)
- `src/layouts/`, `src/pages/`
- `astro.config.mjs`, `package.json`, `wrangler.toml`
- `public/` 전체
- 기존 글 (오타 수정 요청을 따로 받은 경우 제외)

`.env`는 절대 커밋하지 않습니다. API 키를 글 안에 넣지 않습니다.

## 커밋

- 메시지는 한국어
- 콘텐츠 추가는 `content: {주제} 글 N편 추가` 형식
- 한 커밋에 한 주제 묶음
